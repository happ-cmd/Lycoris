# Requirements
- We need one whole config list that we can read and write to
    - Must be able to list both internal and config timings
    - Config must not contain internal timings
- Timing array keys must be their timing IDs
- Timings must not have duplicate names
